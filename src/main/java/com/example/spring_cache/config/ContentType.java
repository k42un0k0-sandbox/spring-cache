package com.example.spring_cache.config;

class ContentType {
    static final String JSON = "application/json";
    // TODO: 画像を送れない可能性があるので、ここの値はバリエーションが必要な場合がある
    static final String FORM_DATA = "application/x-www-form-urlencoded";
}
